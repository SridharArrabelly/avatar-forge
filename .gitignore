# Secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# uv
# (uv.lock IS committed)

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build / dist
dist/
build/

# Logs
*.log

# Conversation audit trail written by AUDIT_SINK=file (backend/audit/sinks.py).
# Real user questions and assistant answers, and *.log above does not match it.
# This repo is PUBLIC — these must never be committed.
audit-log*.jsonl

# azd local state (per-developer env, deployment plan)
.azure/

# Routing harness transcripts (prompts/agent/routing-test-questions.md)
answers_*.txt

# Internal policy corpus. This repo is PUBLIC and these are real corporate
# documents, so they are supplied locally and never committed. Indexing walks
# the filesystem (not git), so ignoring them does not affect the corpus build —
# see data/README.md.
data/policies/

